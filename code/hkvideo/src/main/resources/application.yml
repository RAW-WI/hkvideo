# 配置数据源
spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/hkvideo
    username: root
    password: 123456
  servlet:
    multipart:
      max-file-size: 100MB      # 允许上传单个文件限制
      max-request-size: 200MB   # 最大请求提交数据量